name = Comment Hide Subject
description = Hides the comment subject if the "Allow custom title for comment" checkbox is unchecked for that node type.
package = Comments
type = module
backdrop = 1.x

tags[] = Content

dependencies[] = comment
